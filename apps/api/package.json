{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "uv sync && source ./.venv/bin/activate && uv run prisma generate && uv run fastapi dev ./src/main.py",
    "generate": "docker run --rm -v ${PWD}:/local openapitools/openapi-generator-cli generate -i http://host.docker.internal:8000/openapi.json -g dart-dio -o /local/generated/dart/ai_chat_client --additional-properties=pubName=ai_chat_client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
